'use strict';

/**
 * Hide blocks
 */
/*
function insertAfter(newNode, referenceNode) {
    referenceNode.parentNode.insertBefore(newNode, referenceNode.nextSibling);
}

// https://stackoverflow.com/a/19170853/929437
let titles = document.getElementsByClassName('title');
for (let i = 0, l = titles.length; i < l; i++) {
    const checkbox_id = 'dzzrpp_hide'+i;
    titles[i].setAttribute('for', checkbox_id);
    titles[i].classList.add('collapse');
    let checkbox = document.createElement('input');
    checkbox.setAttribute('id', checkbox_id);
    checkbox.setAttribute('type', 'checkbox')
    insertAfter(checkbox, titles[i])

    //images[i].src = 'http://placekitten.com/' + zads[i].width + '/' + images[i].height;
}
*/


/**
 * Limit the width of the log
 */

//let tables = document.getElementsByTagName('table');



/**
 * Table for KO instead of list
 */
/*
let strongs = document.getElementsByTagName('strong');
for (let i = 0, l = titles.length; i < l; i++) {
    if (strongs[i].text === 'Коды сложности') {
        //...
    }
}
*/
